# Poker777 — สรุปการแก้ไข (รอบนี้)

ฐานโค้ด: `Poker777-MyFriendLastVer` (เวอร์ชันล่าสุดของเพื่อน)
หลักที่ใช้: **แก้ Frontend ได้เต็มที่ / แตะ Backend เฉพาะที่เป็นบัคจริงเท่านั้น** ทุกจุดที่แก้ใน Backend มีคอมเมนต์ `[แก้บัค]` กำกับไว้ ค้นหาได้ด้วย `grep -rn "แก้บัค" backend/src`

---

## 1. Backend — แก้เฉพาะบัคจริง (8 จุด)

| # | ไฟล์ | บัค | ผลกระทบ | วิธีแก้ |
|---|---|---|---|---|
| 1 | `services/tables.js` | `roomNotFoundError()` ถูกคอมเมนต์ทิ้งแต่ยังถูกเรียก | ใส่รหัสห้องผิด → Server ตอบ 500 (ReferenceError) แทน 404 | เปิดฟังก์ชันกลับมา |
| 2 | `services/tables.js` | นับที่นั่งจาก `table:{id}:seats` ซึ่งไม่มีใครเขียน | หน้า Rooms แสดง 0 คนเสมอ, เช็คห้องเต็มผ่าน REST ไม่ทำงาน | นับจาก `room:{room_code}:seats` (Hash ที่ ws-server ใช้จริง) |
| 3 | `ws/ws-server.js` | Sync เงินลง DB เทียบกับ `buyIn` ตั้งต้นทุกมือ แต่ `buyIn` ไม่เคยขยับ | **เงินผิด:** กำไร/ขาดทุนของมือก่อนถูกนับซ้ำทุกมือ (ชนะมือแรก +50 → มือต่อไปได้ +50 อีก) | หลัง Settle แต่ละมือ ตั้ง `buyIn = chips` ปัจจุบัน (`resetSettlementBaseline`) |
| 4 | `services/lockService.js` | ขอ Lock ไม่ได้ throw ทันที | Action ชนกันแล้วหาย ("Room is busy"), การหลุดเน็ตที่ชนกับงานอื่นหาย → ที่นั่งผีไม่ถูกลบ | รอแล้วลองใหม่ ~2 วินาที (retry + jitter) |
| 5 | `ws/ws-server.js` | Timer เปลี่ยน Turn แต่ไม่อัปเดต `currentTurnSeat` | คนที่หมดเวลาไปแล้วกดออก → ระบบเข้าใจผิดว่าเป็น Turn ของเขา แล้วข้าม Turn คนถัดไป | อัปเดต `currentTurnSeat` ด้วย |
| 6 | `services/roomService.js` | `advanceTurn` เมื่อคนที่ถือ Turn ออก เริ่มนับจาก index 0 | ลำดับ Turn ผิด (ข้ามคนเก้าอี้แรก) | นับต่อจากเก้าอี้ของคนที่ออก (`fromSeatKey`) |
| 7 | `ws/ws-server.js` | ตอนคนออก ส่งแค่ `next_turn` | ถ้าการออกทำให้เปิดไพ่กลาง/เปลี่ยน Host → Client ไม่เห็นไพ่กลางใหม่และไม่รู้ว่าใครเป็น Host | ส่ง Snapshot โต๊ะ (`publishRoomState`) หลังมีคนออก |
| 8 | `ws/ws-server.js` | ไม่มี WebSocket Heartbeat | เน็ตค้างแบบ Half-open (มือถือเปลี่ยนเสา/Wi-Fi ค้าง) → Server ไม่รู้ว่าหลุด ผู้เล่นไม่ถูกลุกจากโต๊ะ โดน Auto-fold ทุกมือ, บน AWS ALB ตัด Connection ที่ idle เกิน 60 วินาที | Ping ทุก 15 วินาที ไม่ตอบ Pong → ตัดทิ้ง → เข้าระบบ Reconnect grace ปกติ |

ไฟล์เพิ่มเติม: `backend/.dockerignore` — กัน `node_modules` ของ Windows ถูก COPY เข้า Docker image (Linux)

---

## 2. Frontend

### 2.1 หน้าโต๊ะ `PokerTablePage.jsx` — เขียนใหม่ให้ตรงกับ Protocol ของ Backend
หน้าเดิมเล่นจริงไม่ได้เลย:

| ปัญหาเดิม | แก้เป็น |
|---|---|
| ส่ง `START_GAME` / `GAME_ACTION` / `LEAVE_ROOM` แต่ Backend รับ `start-game` / `game-action` / `leave-room` (ปุ่มกดแล้วไม่เกิดอะไร) | ส่งชื่อที่ Backend รับ |
| รอฟัง `TABLE_STATE` ตัวพิมพ์ใหญ่ แต่ Backend ส่ง `table_state` และส่งแบบ Event ทีละอย่าง (`game-action`, `deal-flop`, `turn-start`, `winner`, …) | รองรับทั้ง Snapshot และ Event ทุกตัว (reducer เดียว) |
| เลขที่นั่งเพี้ยน 1 ช่อง (Backend ส่งเริ่มที่ 1) เก้าอี้สุดท้ายไม่แสดง | แปลงเป็นเริ่มที่ 0 |
| ไพ่ 10 แสดงเป็น `T` และคำนวณมือผิด | แปลง `T` → `10` |
| ปุ่ม BET/RAISE ใช้ผิดจังหวะ, CHECK/CALL กดได้ตลอด | สลับ BET↔RAISE อัตโนมัติ, เปิด CHECK หรือ CALL ตามยอดที่ต้องจ่าย, ปุ่ม CALL บอกยอด, บังคับ Min raise, เพิ่มปุ่ม ALL IN |
| ปุ่ม START ไม่ขึ้น (เช็ค `phase === 'WAITING'` แต่ห้องใหม่ไม่มี phase) | ขึ้นเมื่อเป็น Host, ไม่มีมือที่เล่นอยู่, และมีผู้เล่นมีชิป ≥ 2 คน |
| Buy-in = ขั้นต่ำเสมอ | หน้าต่าง **Take a seat** เลือก Buy-in ด้วยแถบเลื่อน (ขั้นต่ำ – min(สูงสุด, Wallet)) |
| — | Showdown เปิดไพ่คู่แข่ง + ไฮไลต์ผู้ชนะ, แสดง Dealer / สถานะ / ยอด Bet ของแต่ละคน |
| เห็นตัวเองซ้ำบนโต๊ะ | ตัวเองแสดงเฉพาะแผงด้านล่าง (พร้อม Dealer / สถานะ / Bet) |
| วงนับเวลาสีชมพูค้างที่ 70% (ค่าตายตัวใน CSS) | วงลดลงตามเวลาจริง (15 วิ), 5 วินาทีสุดท้ายเป็นสีแดง, แถบในแผงด้านล่างนับถอยหลังเมื่อถึงตาเรา |
| รองรับที่นั่งแค่ 6 ตำแหน่ง (Lobby ให้สร้าง 9 ที่) | เพิ่มตำแหน่งที่นั่ง 6–8 |

### 2.2 Reconnect / เน็ตไม่เสถียร
| สถานการณ์ | พฤติกรรม |
|---|---|
| Refresh หรือเน็ตหลุด < 30 วินาที | กลับมานั่งที่เดิม ไพ่เดิม ชิปเดิม อัตโนมัติ + แจ้ง "Reconnected" |
| หลุดระหว่างตาตัวเอง | Server Auto-check/fold ตามเดิม กลับมาเล่นต่อได้ |
| หลุด > 30 วินาที | แจ้งว่าหลุดนานเกินไป ชิปคืน Wallet แล้ว และให้เลือก Buy-in ใหม่ (เดิมนั่งใหม่เงียบๆ พร้อมหัก Buy-in ใหม่โดยไม่ถาม) |
| เน็ตค้างแบบ Half-open | Watchdog: เลยเวลา Turn 6 วินาทีแล้วไม่มีข้อมูลมา → ต่อใหม่เอง, Timeout การเชื่อมต่อ 8 วินาที, ฟัง Event online/offline ของ Browser |
| ระหว่างหลุด | แถบเหลือง "Connection lost — reconnecting…" ด้านบน (เดิมผู้เล่นไม่รู้ตัวว่าหลุด) |
| ยอด Bet หลัง Reconnect | ยอด Bet ของตัวเองยังถูกต้อง (หลุดแล้วลงชิปไม่ได้), ของคนอื่นซ่อนจนถึงรอบถัดไป (กันแสดงค่าเก่า) |

### 2.3 เสียงเอฟเฟกต์ `frontend/src/lib/sound.js` (ใหม่)
สร้างเสียงด้วย Web Audio API ไม่มีไฟล์เสียง ไม่มีปัญหาลิขสิทธิ์ — แจกไพ่, เปิดไพ่กลาง, ลงชิป, All-in, Check, Fold, ถึงตาเรา, นับถอยหลัง 5 วินาทีสุดท้าย, มีคนเข้า/ออก, ชนะ, แพ้, Error — ปุ่ม 🔊/🔇 บนหัวโต๊ะ (จำค่าไว้)

### 2.4 Lobby / Rooms
- ปุ่ม Join / Enter Table เดิมพาไป `/test.html` ซึ่ง **ไม่ถูก build ไปกับ Vite** → บน Production จะเด้งกลับหน้า Login → เปลี่ยนเป็น `/poker-table`
- เปลี่ยนป้าย "Blinds min/max" เป็น **"Buy-in min – max"** เพราะ `min_bet` / `max_bet` ใน Backend คือช่วง Buy-in ส่วน Blinds มาจาก `.env` (`POKER_SMALL_BLIND` / `POKER_BIG_BLIND`, ค่าเริ่มต้น 10/20)

### 2.5 เครื่องมือทดสอบ `tools/demo-server/` (ใหม่, ไม่เกี่ยวกับ Production)
รัน Backend ตัวจริงโดยไม่ต้องมี Docker/MySQL/Redis (ใช้ Redis/MySQL จำลองใน RAM) + Proxy จำลองเน็ตไม่เสถียร — วิธีใช้ดู `test_host.md` (ลบทั้งโฟลเดอร์ได้ถ้าไม่ต้องการ)

---

## 3. การทดสอบที่ทำแล้ว

| ชุดทดสอบ | ผล |
|---|---|
| `node --test src/test/game.test.js` | ผ่าน 6/6 |
| `vite build` | ผ่าน |
| Script 2 ผู้เล่น 3 มือ (Demo server) | Wallet ถูกต้อง ไม่นับซ้ำ (ผลรวมเงินคงที่ 2,000), รหัสห้องผิด → 404, นับที่นั่งถูก, ห้องร้างถูกลบ |
| Browser 2 ผู้เล่น | สร้างห้อง → Buy-in → เล่นจน Showdown, Timeout, Refresh กลางมือ, Leave กลางมือ, Wallet ถูกต้อง |
| Reconnect ผ่าน Proxy จำลองเน็ต | หลุด 5 วิ / หลุดตอนตาตัวเอง 20 วิ / หลุด 40 วิ / Half-open 25 วิ — ผ่านทั้งหมด |

**ยังไม่ได้ทดสอบ:** กับ MySQL / Redis ตัวจริง (Docker), บน AWS, ชุดทดสอบ `auth / wallet / tables` (ต้องใช้ MySQL)

---

## 4. เรื่องที่เพื่อนต้องตัดสินใจ (พบแต่ยังไม่แก้ เพราะเป็น Backend / เป็นการออกแบบ)

| # | เรื่อง | รายละเอียด | ความรุนแรง |
|---|---|---|---|
| 1 | **เงินไม่ถูกกันไว้ตอนนั่ง (ไม่มี Escrow)** | ตอนนั่งแค่เช็คยอด Wallet ไม่ได้หักจริง → เอาเงินก้อนเดียวไปนั่งหลายโต๊ะได้ และถ้าเสียเกินยอดใน Wallet ตอน Settle จะ `INSUFFICIENT_BALANCE` แล้วยอดเสียนั้นหายไป (log แค่ error) | สูง |
| 2 | Host คือคนที่เชื่อม WebSocket เข้าห้องคนแรก | ไม่ใช่คนสร้างห้อง (`tables.host_id`) ถ้าเพื่อนเข้าโต๊ะก่อนคนสร้าง เพื่อนจะได้เป็น Host | ต่ำ |
| 3 | ห้องที่สร้างแล้วไม่มีใครเข้าเลย | อยู่ในรายการห้องตลอดไป (ห้องถูกลบเฉพาะตอนคนสุดท้ายออก) | ต่ำ |
| 4 | Half-open ช่วงที่ **ไม่มีมือกำลังเล่น** | Server จัดการได้แล้ว (Heartbeat) แต่หน้าเว็บของคนที่หลุดจะไม่รู้ตัวจนกว่าจะ Refresh — แก้เต็มต้องเพิ่มข้อความ ping ระดับ App ใน Backend | ต่ำ |
| 5 | ผู้เล่นชิปหมดแต่ยังนั่งอยู่ ถูกนับเป็นผู้เล่นที่ยังไม่หมอบ | เงินไม่ผิด แต่ถ้าคนอื่นหมอบจนเหลือคนเดียว มือจะเล่นไปจน Showdown แทนที่จะจบทันที | ต่ำ |
| 6 | ชิปหมดต้องลุกแล้วนั่งใหม่ | ยังไม่มีปุ่ม Rebuy | ฟีเจอร์ |
| 7 | คนเดียวเปิดหลายแท็บโต๊ะเดียวกัน | แท็บใหม่จะเตะแท็บเก่าออก (Backend ปิด Socket เก่า) — ถือว่าโอเค แต่ควรแจ้งผู้เล่น | ต่ำ |
| 8 | ค่า Redis | `config/redisClient.js` Hard-code port 6379 และปิด TLS ไว้ (คอมเมนต์ `tls: {}`) — ถ้า ElastiCache เปิด In-transit encryption ต้องเปิด | Deploy |
| 9 | RDS Read-replica | มีใน Diagram แต่โค้ดใช้แค่ Primary | Deploy |
| 10 | ไฟล์เก่าที่ไม่ได้ใช้ | `ws/ExServer.js`, `ws/Old-server.js`, `services/ExRoomService.js`, และฟังก์ชันใน `roomService.js` ที่ไม่มีใครเรียก (`setPlayerCards`, `getCommunityCards` ฯลฯ), `frontend/test.html` | ทำความสะอาด |
